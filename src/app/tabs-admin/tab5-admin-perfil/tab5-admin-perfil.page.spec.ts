import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab5AdminPerfilPage } from './tab5-admin-perfil.page';

describe('Tab5AdminPerfilPage', () => {
  let component: Tab5AdminPerfilPage;
  let fixture: ComponentFixture<Tab5AdminPerfilPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab5AdminPerfilPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

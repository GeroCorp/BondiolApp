import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab2CargaMesasPage } from './tab2-carga-mesas.page';

describe('Tab2CargaMesasPage', () => {
  let component: Tab2CargaMesasPage;
  let fixture: ComponentFixture<Tab2CargaMesasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab2CargaMesasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

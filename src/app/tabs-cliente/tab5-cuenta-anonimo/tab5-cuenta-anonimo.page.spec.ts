import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab5CuentaAnonimoPage } from './tab5-cuenta-anonimo.page';

describe('Tab5CuentaAnonimoPage', () => {
  let component: Tab5CuentaAnonimoPage;
  let fixture: ComponentFixture<Tab5CuentaAnonimoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab5CuentaAnonimoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab3ConsultasPage } from './tab3-consultas.page';

describe('Tab3ConsultasPage', () => {
  let component: Tab3ConsultasPage;
  let fixture: ComponentFixture<Tab3ConsultasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab3ConsultasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

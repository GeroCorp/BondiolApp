import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab4HistorialPage } from './tab4-historial.page';

describe('Tab4HistorialPage', () => {
  let component: Tab4HistorialPage;
  let fixture: ComponentFixture<Tab4HistorialPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab4HistorialPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

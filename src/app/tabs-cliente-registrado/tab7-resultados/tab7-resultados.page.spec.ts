import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab7ResultadosPage } from './tab7-resultados.page';

describe('Tab7ResultadosPage', () => {
  let component: Tab7ResultadosPage;
  let fixture: ComponentFixture<Tab7ResultadosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab7ResultadosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
